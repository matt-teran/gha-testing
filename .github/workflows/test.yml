name: Do something!
on:
  pull_request_target:
    types: [ready_for_review]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 20
      - uses: "./"
